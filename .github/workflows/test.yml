name: Utils Tests

on:
  push:
    branches: [ trunk ]
  pull_request:
    branches: [ trunk ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: Change directory
      run: cd util/formulae_builder

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
